# podup apt repository — installed at /etc/apt/sources.list.d/podup.sources by
# the podup-archive-keyring package. deb822 format (apt >= 1.1, all supported
# Debian/Ubuntu releases).
Types: deb
URIs: https://apt.glyndor.net
Suites: stable
Components: main
Architectures: amd64
Signed-By: /usr/share/keyrings/podup.gpg
